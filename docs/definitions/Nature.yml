---
definitions:
  Nature:
    type: object
    properties:
      name:
        type: string
      up:
        type: string
      down:
        type: string
      flavor:
        type: string
      usage:
        type: string
    required:
      - name
      - up
      - down
      - usage
