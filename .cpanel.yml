---
deployment:
  tasks:
    # export variables
    - export DEPLOYPATH=/home/pokerom/public_html
    - export NPMPATH=/usr/bin/npm
    - export NODE_ENV=production
    # run commands
    - /bin/cp www/.htaccess $DEPLOYPATH
    - /bin/cp www/languages.php $DEPLOYPATH
    - /bin/cp www/info.html $DEPLOYPATH
    - $NPMPATH run pack
    - $NPMPATH restart
