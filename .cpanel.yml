---
deployment:
  tasks:
    # export variables
    - export DEPLOYPATH=/home/pokerom/public_html
    - export NPMPATH=/usr/bin/npm
    - export NODE_ENV=production
    # run commands
    - /bin/cp .htaccess $DEPLOYPATH
    - /bin/cp languages.php $DEPLOYPATH
    - $NPMPATH run pack
    - $NPMPATH restart
